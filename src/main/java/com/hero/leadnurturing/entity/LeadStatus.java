package com.hero.leadnurturing.entity;

/**
 * Lead status enum
 */
public enum LeadStatus {
    NEW,            // Newly created lead
    QUALIFIED,      // Lead is qualified
    UNQUALIFIED,    // Lead is not qualified
    LOST            // Lead is lost
}